#!/usr/bin/env ruby

# Load the Rails environment so importmap commands that reference Rails
# (e.g., to read config) have the Rails constant available.
require_relative "../config/environment"
require "importmap/commands"

Importmap::Commands.start
